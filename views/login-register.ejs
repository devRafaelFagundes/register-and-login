<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/login-register.css">
    <title>Login page</title>
</head>
<body>
    <div id="loginContainer" class="halfScreen">
        <h1 class="title" id="titleLogin">Login</h1>
        <p class="subtitle">Either login or register to access the main page. <br>
        Make the difference, make with us.</p>
        <form id="login" class="forms">
            <!-- wont use action="/login", since i will need to use fetch -->
            <label for="username-login">Username</label>
            <input type="text" id="username-login" name="username">
            <label for="email-login">Email</label>
            <input type="email" id="email-login" name="email">
            <label for="password-login">Password</label>
            <input type="password" id="password-login" name="password">
        </form>
        <button id="loginButton" type="submit" form="login" class="button">Log in</button>

    </div>
    <div id="registerContainer" class="halfScreen">
        <h1 class="title" id="titleRegister">Register</h1>
        <p class="subtitle" id="registerSubtitle">Either login or register to access the main page. <br>
            Make the difference, make with us.</p>
        <form id="register" class="forms">
            <!-- wont use action="/register", since i will need to use fetch -->
            <label for="username-register">Username</label>
            <input type="text" id="username-register" name="username">
            <label for="email-register">Email</label>
            <input type="email" id="email-register" name="email">
            <label for="password-register">Password</label>
            <input type="password" id="password-register" name="password">
            <p id="error"></p>
        </form>
        <button type="submit" form="register" id="registerButton" class="button">Register</button>

    </div>
    <script src="/js/sendData.js"></script>
</body>
</html>